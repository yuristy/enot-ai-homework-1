# AGENTS.md

Правила проекта. Этот файл читает ваш AI-ассистент — Claude Code (через `@AGENTS.md`
в `CLAUDE.md`), Codex, Cursor и другие инструменты, понимающие эту конвенцию.

Файл состоит из двух частей. **Часть I** — правила курса, они одинаковы для всех
и менять их не нужно. **Часть II** — настройки вашего проекта, её вы дописываете
под свою задачу. Места для заполнения помечены `[...]`.

---

# Часть I. Правила курса

## 1. Как ведётся работа

Шесть правил с первого занятия. Они обязательны — и именно их следы
мы ищем в журнале работы.

**Свой файл правил.** Часть II этого файла заполняете вы, под свою задачу.
Написать черновик может агент, но принимать — вам: прочитайте глазами
и вычеркните то, чего в проекте нет.

**Режим разрешений выбирается осознанно.** Не работайте в режиме, где агент
не спрашивает ничего, только потому, что так меньше кликов. Зафиксируйте
в журнале выбранный режим и почему именно он.

**Сначала описание и план, потом работа.** Прежде чем агент начнёт править файлы,
у вас на руках должны быть записанное описание задачи и план. Их правит человек —
это самая дешёвая точка вмешательства.

**Проверка вместо слова «готово».** Утверждение агента, что всё работает,
доказательством не является. Доказательство — запуск с показанным выводом:
тесты, открытая страница, снимок экрана. В журнале нужны случаи, где агент
сказал «готово», а проверка это опровергла.

**Независимый взгляд.** Хотя бы раз пропустите результат через проверку со стороны:
вторая модель, чистая сессия без истории обсуждения или отдельный агент-ревьюер.
Что нашлось — в журнал.

**Одна задача — одна сессия.** Новая задача начинается в новой сессии.
Если третья попытка починить одно и то же снова не удалась — не продолжайте
в том же диалоге, начните заново с нормальным описанием проблемы.

Подробности по каждому пункту — в материалах занятия, папка `materials/`.

## 2. Журнал работы

Ход работы фиксируется на диске и сдаётся вместе с решением. Мы оцениваем
не только то, что получилось, но и то, как вы к этому шли. Готовый код
без журнала — неполная сдача.

Если работаете без AI-ассистента, ведите журнал руками. Требования те же.

### Где лежит

В корне проекта — папка `sessions/` (допустимо `Сессии/`). Внутри — файл на каждую
сессию работы. Сессия — это один подход к задаче: сели, поработали, закрыли ноутбук.

```
sessions/
├── STATE.md              # состояние проекта «как есть» (перезаписывается)
├── TOOLS.md              # журнал инструментов (только дополняется)
├── session-1.md
├── session-2.md
└── session-N.md
```

Код в журнале не дублируется — он лежит там, где ему место. В журнале только то,
чего в коде не видно: что просили, что думали, чем закончилось.

**В начале сессии:** прочитать `STATE.md`, определить номер новой сессии
как `max(существующих) + 1`, создать файл сессии **до** первой правки кода.

### Файл сессии

Обязательные разделы, в этом порядке.

```markdown
# Сессия N — <краткое название>

- **Дата:** YYYY-MM-DD
- **Ассистент:** <инструмент и модель, или «без ассистента»>
- **Режим разрешений:** <какой и почему>
- **Статус:** в работе | завершена | прервана

## Промпты
Все запросы к ассистенту, по порядку, дословно.

## Размышления
Какие варианты рассматривались, что отвергнуто и почему, какие допущения приняты,
где была неопределённость и как её сняли.

## Использованные инструменты
| Инструмент | Действие | Зачем |

## Изменения в проекте
Созданные / изменённые / удалённые файлы, по строке на файл.

## Финальный вердикт
Что сделано, что осталось, что сломано. Явно: пункт закрыт или нет.
```

**Промпты копируются как есть** — без исправления опечаток, без перевода,
без склейки нескольких в один. Отредактированный промпт — уже не данные, а пересказ.

**Размышления — главный раздел.** Нас интересует не «сделал A, потом B»,
а почему A, а не C; что вы поняли не сразу; где ассистент ошибся и как вы это
заметили; какое место в задании оказалось неоднозначным и как вы его истолковали.

Плохо:

> Реализовал эндпоинт. Добавил тесты. Всё работает.

Хорошо:

> Не было ясно, что делать при повторной отправке той же записи. Вариантов два:
> 409 или идемпотентный 200. Выбрал идемпотентность — клиент мобильный, ретраи
> по таймауту неизбежны. Записал допущение в README. Ассистент сначала предложил
> хранить ключ идемпотентности в памяти процесса — при двух репликах ломается,
> перенёс в хранилище с TTL.

**Когда писать.** Промпт — сразу при отправке. Размышления и инструменты — по ходу.
Вердикт — в конце. Журнал, восстановленный по памяти вечером последнего дня,
видно сразу, и он не засчитывается.

### `TOOLS.md` — журнал инструментов

Только дополняется, старые записи не редактируются. Фиксируется всё, что вы добавили
в проект или окружение ради этой задачи: библиотеки, CLI-утилиты, линтеры,
MCP-серверы, плагины и скиллы ассистента, хуки, внешние сервисы, контейнеры.

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y
- **Тип:** библиотека / CLI / MCP / плагин / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Проверка:** чем убедились, что работает
```

Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

### `STATE.md` — состояние проекта

Обновляется в конце каждой сессии, описывает «как есть сейчас», а не историю:
что готово, что в работе и где именно остановились, что не начато,
какие инструменты используются, известные проблемы, следующий шаг.

## 3. Что мы читаем в журнале

- **Постановка.** Заметили ли вы неоднозначности в задании и как их разрешили.
- **Выбор.** Есть ли отвергнутые варианты. Решение без альтернатив обычно означает,
  что выбора и не было — взяли первое, что пришло в голову.
- **Работа с ассистентом.** Проверяете ли вы то, что он выдал. Ловите ли ошибки.
  Или принимаете код, который не можете объяснить.
- **Честность.** Тупики, откаты и «сделал не так, переделал» повышают оценку.
  Идеально гладкий журнал выглядит выдуманным.
- **Границы.** Понимаете ли вы, что осталось за рамками и чем это грозит.

Журнал не проверяется на объём. Три честных абзаца лучше трёх страниц воды.

## 4. Чего не делать

- Не переписывать журнал задним числом «чтобы красивее выглядело».
- Не генерировать журнал одним промптом в конце — это видно по языку
  и по отсутствию тупиков.
- Не коммитить секреты и ключи, в том числе внутри промптов.
- Не складывать в журнал код, дампы и логи — только журнал.

---

# Часть II. Настройки проекта

Дальше — ваша часть. Заполните её под свою задачу до того, как агент начнёт работать.
Пустые заготовки ниже — не образец для подражания, а места, где нужны ваши решения.

## Что это за проект

Веб-экран с географической картой Москвы и развлекательными мероприятиями
(концерты, фестивали, выставки), с фильтром по дате (календарь, один день) и
категории. Данные — статичный снапшот KudaGo API, без бэкенда. Полная спека —
`SPEC.md`.

## Стек и версии

- **React 19 + TypeScript + Vite** (последние стабильные версии на момент
  установки — зафиксировать точные номера в `package.json` после `npm create
  vite@latest` и не апгрейдить мажорно без вопроса).
- **Leaflet + `react-leaflet`** — npm-зависимости, тайлы OpenStreetMap. При
  установке проверить, что версия `react-leaflet` заявляет React 19 как peer
  dependency. Если последняя стабильная тянет React 18 — не ставить через
  `--legacy-peer-deps`/`--force` молча, сообщить и предложить варианты
  (более новая версия `react-leaflet`, либо `leaflet` напрямую без
  React-обёртки).
- **Node.js — установленная версия в среде разработки v26.5.0, npm 11.17.0.**
  В `package.json` (`engines`) фиксировать только нижнюю границу мажорной
  версии как рекомендацию (например, `>=20`), не точный патч — на чистой
  машине проверяющего почти наверняка другой патч, а жёсткий пин скорее
  сломает установку, чем защитит от неё.
- **После первой установки — зафиксировать точные версии зависимостей.**
  В `package.json` без диапазонов (`^`/`~`), точные `x.y.z`. Закоммитить
  `package-lock.json`. Дальше локальную установку делать через `npm ci`, а
  не `npm install` — дерево зависимостей не должно «плыть» между запусками.
- Смена стека, сборщика или способа подключения карты — только с вопросом
  человеку (см. также запрет на новые зависимости ниже — CDN-скрипт в обход
  `package.json` это тоже новая зависимость).

## Как запускать и проверять

```bash
# установка зависимостей (первый раз)
npm install
# установка зависимостей (повторно / на чистой машине, после появления lock-файла)
npm ci

# запуск — строго порт 5173, http://localhost:5173
npm run dev -- --port 5173 --strictPort

# сборка
npm run build

# линтер
npm run lint

# автотестов на старте нет — проверка ручная (см. ниже) и/или Playwright MCP
```

Эти команды агент выполняет сам, когда нужно показать результат.
Не заставляйте его гадать.

## Что нельзя трогать

- Ни при каких обстоятельствах не подключать бэкенд, базу данных или
  прокси-сервер: источник данных — файл-снапшот в репозитории.
- Ни при каких обстоятельствах не заменять снапшот живыми запросами к KudaGo
  в рантайме приложения.
- Не удалять и не перезаписывать файл снапшота и скриншоты «до» дизайна —
  без них нечем подтвердить сравнение прогонов.
- Не менять версии React/Vite/Leaflet и не добавлять новые зависимости без
  отдельного вопроса — ни npm-пакетом, ни `<script>` с CDN в обход
  `package.json`: с точки зрения этого правила оба способа новая зависимость.
- Не выполнять необратимые команды: удаление каталогов, принудительный push,
  очистку истории git.
- Не читать и не выводить содержимое файлов с секретами (если такие
  появятся — их в проекте быть не должно).
- Не читать `data/events-snapshot.json` напрямую из компонентов карты/фильтров
  — только через модуль-загрузчик (см. «Соглашения проекта»). Это тот самый
  шов, через который в будущем подключается живой API, не размазывать его по
  экрану.

Помните: правило в файле — это просьба. Механизм — режим разрешений
и запреты в настройках инструмента. Одно без другого не работает.

## Когда агент обязан остановиться и спросить

- Требование `SPEC.md` допускает два прочтения (например, поведение при
  нескольких мероприятиях в одной точке карты).
- Нужно принять решение, которое трудно откатить: смена источника данных,
  структуры снапшота, способа подключения карты.
- Предлагается добавить зависимость, которой ещё нет в `package.json`
  (включая CDN-скрипт — см. «Что нельзя трогать»).
- Объём работы вырос против `SPEC.md`: новый экран, новый фильтр, новая
  сущность.
- Данные в снапшоте KudaGo не позволяют честно показать одно из трёх
  состояний (например, нет ни одной пустой комбинации дата+категория для
  состояния «Ничего не найдено») — сообщить, а не подгонять снапшот вручную
  без пометки об этом.
- Правка логики экрана походя цепляет оформление, или наоборот — остановиться
  и спросить, продолжать ли в этом же промпте или разнести на два: дизайн
  прогоняется отдельно от разработки (новой сессией, отдельным промптом),
  иначе потом не разобрать, что дал дизайн, а что обычная доводка.

## Состояния — контракт, а не разовая проверка

Три состояния экрана — «Пусто», «Есть данные», «Ничего не найдено» (описаны в
`SPEC.md`) — проверяются **после каждой правки, которая касается экрана**, а
не только перед финальным ответом и не только то состояние, которое
чинили. Починили баг в «Есть данные» — проверьте, что «Пусто» и «Ничего не
найдено» не сломались попутно.

## Что проверить перед финальным ответом

- Запустить `npm run build` и `npm run lint`, показать вывод целиком, включая
  ошибки.
- Поднять `npm run dev -- --port 5173 --strictPort` и показать, что страница
  открылась на `http://localhost:5173`.
- Пройти руками все три состояния («Пусто», «Есть данные», «Ничего не
  найдено») и один нештатный сценарий (испорченный `localStorage`).
- Проверить экран на ширинах 360 и 1440 px — без горизонтальной прокрутки.
- Пройти клавиатурой: `Tab` по всем интерактивным элементам, `Enter`/`Space`
  срабатывают, `Esc` закрывает карточку мероприятия.
- Открыть консоль браузера и убедиться, что она чистая — без ошибок и
  красных предупреждений.
- Приложить скриншоты «до / после» улучшения дизайна на одной ширине с
  одними данными.
- Перечислить изменённые файлы и убедиться, что снапшот и скриншоты «до» не
  тронуты лишний раз.

Фраза «всё работает» без вывода команды или скриншота ответом не считается.

## Соглашения проекта

- Код и комментарии — на английском, тексты интерфейса — на русском (аудитория
  — Москва).
- Структура: `src/components`, `src/data` (снапшот и загрузка), `src/hooks`.
- **Доступ к данным мероприятий — через единственный модуль**
  (`src/data/events.ts` или аналог), который сегодня статически импортирует
  `data/events-snapshot.json` и отдаёт наружу собственный тип `Event`,
  отличный от сырого формата ответа KudaGo. Компоненты, карта и фильтры
  работают только с этим типом и не знают о формате KudaGo вообще. Переход
  на живой API в будущем — замена тела этого одного модуля (снапшот →
  `fetch` + маппинг в тот же `Event`), а не переписывание экрана.
- Коммиты — короткое сообщение в повелительном наклонении на русском,
  осмысленными порциями, а не одним коммитом на весь проект.
