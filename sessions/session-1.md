# Сессия 1 — Контекст, спецификация, наивный и spec-прогон (Frontend: карта мероприятий Москвы)

- **Дата:** 2026-08-15 — 2026-08-17
- **Ассистент:** Claude Code, Sonnet 5
- **Режим разрешений:** стандартный, с подтверждением на инструменты (не
  auto-accept). Выбран осознанно — задание курса именно про контроль над
  агентом, а не про скорость; один раз в ходе сессии я отклонил вызов
  инструмента и подправил ответ вручную (см. ниже) — это и есть пример того,
  зачем такой режим нужен.
- **Статус:** завершена

## Промпты

> Все запросы к ассистенту, дословно, по порядку.

### Промпт 1
> cd testtask

### Промпт 2
> yes, read through those

### Промпт 3
> напиши все это на русском

### Промпт 4
> Возьмем Frontend

### Промпт 5 (ответ на AskUserQuestion про тему экрана)
> Карта интересных развлекательных мероприятий в Москве с календарем и фильтром

### Промпт 6 (ответы на пакет из 4 вопросов про спеку)
> «Карта» — географическая карта. Источник данных — снапшот с реального
> открытого источника. Фильтры — категория + дата. Календарь — выбор одного
> дня.

### Промпт 7 (отклонён вызов AskUserQuestion, дан ответ текстом)
> React

### Промпт 8
> React 19

### Промпт 9
> Согласен со всем списком, состояния тоже подходят

### Промпт 10 (ответ на вопрос про Leaflet)
> npm: react-leaflet + leaflet (рекомендую)

### Промпт 11 (ответ на вопрос про TS/JS)
> TypeScript

### Промпт 12
> Создай снапшот до 31 августа

## Размышления

Задачу выбирали из семи предложенных — пользователь сразу предложил свою тему
(карта мероприятий Москвы) вместо готовых вариантов из `homework.pdf`, что
допустимо и даже предпочтительно по правилам задания.

Неоднозначность №1: слово «карта» — географическая карта или каталог карточек?
Не стал гадать, спросил явно — оказалось буквальная карта, это меняет весь
стек (нужна картографическая библиотека).

Неоднозначность №2: источник данных о мероприятиях. Предложил пользователю
выбор между мок-датасетом, снапшотом с реального источника и живым API.
Выбран снапшот с реального источника — тогда нужно было найти рабочий
открытый API. Проверил через `WebFetch` `kudago.com/public-api/v1.4/events/`
до того, как предлагать его пользователю — источник действительно отвечает
без ключа и отдаёт координаты места (`place.coords`), что критично для
географической карты. Не поверил на слово описанию в интернете, проверил
фактическим запросом.

Стек менялся дважды за сессию: сначала предполагался список
HTML/JS-фреймворков, пользователь выбрал React, затем уточнил версию
(React 19). Из-за этого пришлось пересмотреть более раннее решение про
подключение Leaflet — я предлагал CDN-скрипт до того, как узнал про React,
а с бандлером логичнее npm-пакет. Спросил повторно вместо того, чтобы молча
оставить более раннее решение — правило «расхождение — повод спросить»
сработало здесь на собственном черновике, а не только на коде агента.

Отдельно был случай, когда я вызвал `AskUserQuestion` с 4 вопросами сразу
(включая multiSelect про границы задачи) — пользователь отклонил вызов
инструмента и ответил одним словом «React» в чате. Это подтверждает пункт
правил «независимый взгляд»/контроль на практике: даже безобидный вызов
инструмента может быть отклонён, и агент обязан продолжить с тем, что дал
пользователь, а не повторять то же самое.

Процессная ошибка с моей стороны: я отредактировал `SPEC.md` и `AGENTS.md`
до создания файла сессии, хотя сам же зафиксировал в `AGENTS.md`
(унаследовано из Части I) правило «создать файл сессии до первой правки
кода». Обнаружил и исправил в начале следующего шага, зафиксировано здесь
как есть, без причёсывания.

## Использованные инструменты

| Инструмент | Действие | Зачем |
|---|---|---|
| Read | Прочитал README.md, SPEC.md, CLAUDE.md, AGENTS.md, homework.pdf, sessions/*, materials/ listing | Осмотреться в проекте перед началом работы |
| AskUserQuestion | Серия вопросов по спецификации (тип карты, источник данных, фильтры, роль календаря, стек, состояния, границы задачи, Leaflet, TS/JS) | Спецификация должна собираться через вопросы к человеку, а не додумываться агентом |
| WebFetch | Запрос к `kudago.com/public-api/v1.4/events/` дважды — проверка структуры ответа и наличия координат | Не полагаться на память о том, как выглядит API — проверить фактическим запросом перед тем как предлагать источник пользователю |
| Bash (python3) | Расчёт unix-timestamp для диапазона дат 17–31 августа 2026 (Europe/Moscow) | Нужны точные `actual_since`/`actual_until` для запроса к KudaGo |
| Write/Edit | `SPEC.md`, `AGENTS.md` (Часть II) | Зафиксировать спецификацию и правила проекта |

## Изменения в проекте

- `SPEC.md` — заполнен: что делаем, для кого, сценарии, состояния, границы,
  допущения, открытые вопросы с ответами, критерии приёмки.
- `AGENTS.md` — заполнена Часть II: стек, команды, запреты, когда
  останавливаться и спрашивать, чек-лист перед «готово», соглашения.
- `sessions/session-1.md` — этот файл, создан с опозданием (после правки
  SPEC.md/AGENTS.md, а не до неё — см. «Размышления»).
- `sessions/TOOLS.md` — добавлена запись про KudaGo public API.
- `data/events-snapshot.json` — создан: 18 мероприятий Москвы на 17–31 августа
  2026, с координатами, категориями, датой/временем, местом и ссылкой на
  источник; позже перенесён в `src/data/events-snapshot.json` (`git mv`).
- Каркас Vite+React+TS в корне репозитория: `package.json` (+ `package-lock.json`,
  точные версии), `tsconfig*.json`, `vite.config.ts`, `index.html`, `public/`,
  `.oxlintrc.json`.
- `src/data/{types.ts, categories.ts, events.ts}` — типы, справочник категорий,
  единственный модуль-загрузчик данных.
- `src/utils/date.ts` — валидация и арифметика дат без внешних зависимостей.
- `src/hooks/useEventFilter.ts` — состояние фильтра, вычисление экрана
  (`empty`/`has-data`/`not-found`), персист и валидация `localStorage`.
- `src/components/{Calendar,EventsMap,EventCard}.tsx`, `src/App.tsx`,
  `src/App.css`, `src/index.css`, `src/main.tsx` — реализация экрана.
- `.gitignore` — добавлены `dist`, `dist-ssr`, `*.local`, `*.log`.

## Промпт 12 — снапшот: как делался

Промпт «Создай снапшот до 31 августа» был выполнен так:

1. Посчитал unix-диапазон 17–31 августа 2026 по московскому времени —
   без него `actual_since`/`actual_until` пришлось бы подбирать на глаз.
2. Запросил у KudaGo 100 событий с координатами, ограниченных этим диапазоном
   и Москвой (`location=msk`). Всего под фильтр подошло 412 — по протоколу
   курса не гонюсь за «настоящим» полным списком, беру рабочую выборку.
3. Отфильтровал по наличию `place.coords` (без них нечем поставить метку на
   карте — из 100 таких оказалось 74) и по «развлекательным» категориям
   (исключил `education`, `business-events`, `stock`, `other` — тема экрана
   именно развлечения, не образование/бизнес).
4. У KudaGo многие события — повторяющиеся (одно и то же мероприятие с
   десятками дат, включая записи аж с 2015 года и вплоть до плейсхолдерной
   даты `0001-01-03`). Для снапшота это усложнение не нужно: для каждого
   события взял только одно вхождение даты, которое попадает в окно
   17–31 августа 2026 — самое раннее из подходящих. Реального расписания
   повторов снапшот не хранит, это явное упрощение, записано в `meta.note`
   самого файла снапшота.
5. Вручную отобрал 18 из 47 отфильтрованных кандидатов, добиваясь разброса
   по категориям (7 категорий) и по дням (8 разных дней из 15 в диапазоне) —
   так, чтобы часть комбинаций «день + категория» осталась пустой без
   искусственной подгонки: это нужно для честной проверки состояния
   «Ничего не найдено» из `SPEC.md`.
6. Сырой ответ API (100 событий) и промежуточный список кандидатов (47) не
   стал класть в репозиторий — это дамп и черновик, а не часть сдачи;
   сохранены только в скретчпад-директории сессии инструмента.

Отдельно отмечу: снапшот не проверялся визуально на самой карте — карты
как таковой ещё нет, реализация не начата. То, что все 18 точек физически
попадают в Москву и координаты не искажены, будет видно только на этапе
реализации; если окажется, что какая-то метка вылезла за пределы города —
это и будет тем самым «расхождение — повод спросить», а не тихая правка.

## Промпт 13 — перепроверка AGENTS.md против homework.pdf

Прошу перепроверить `AGENTS.md`/`SPEC.md` перед наивным прогоном и указать
незаполненное. Перечитал `homework.pdf` построчно против записанных правил и
нашёл 5 расхождений с требованиями курса для Frontend-задания:
запрет CDN был сужен только до Leaflet вместо общего правила; проверка трёх
состояний стояла только «перед финальным ответом», а не после каждой правки;
порт дев-сервера был «обычно 5173», а не жёстко зафиксирован; отсутствовал
пункт «консоль браузера без ошибок»; не было явного правила не смешивать
правки дизайна и логики в одном промпте.

### Промпт 14
> Да, в частности замени запрет использования CDN библиотек, это не
> критично для проекта. Закрепи строгий порт 5173 и убери "обычно 5173",
> оставь прямую ссылку на этот порт, на которой будет запущен проект. Если
> считаешь, что запрет нельзя убирать, предложи альтернативы, также поправь
> и другие однозначные пункты сейчас.

Решение по CDN: не убрал правило полностью, а свернул в уже существующий
общий запрет «не добавлять новые зависимости без вопроса» — явно указав, что
CDN-скрипт в обход `package.json` тоже считается новой зависимостью. Отдельная
бесповоротная строка про CDN была избыточна именно для этого стека: проект
целиком на npm-зависимостях (React, Vite, Leaflet), отдельного пути для
CDN-библиотек в архитектуре просто нет — риск, для которого курс писал этот
пункт (обычно про vanilla-проекты без сборщика), здесь ниже. Предложил это как
альтернативу полному удалению — пользователь не возражал.

Остальные три пункта поправлены без обсуждения — они однозначные, разночтений
не допускают: порт зафиксирован флагами `--port 5173 --strictPort` вместо
«обычно», добавлена проверка консоли браузера в чек-лист перед ответом,
проверка трёх состояний вынесена в отдельное правило «после каждой правки»,
а не только перед финальным ответом.

### Промпт 15
> Перед наивным прогоном, предложи идеи по лучшим ограничениям и стеку для
> текущего проекта, позаботься о том, чтобы проект было легко и быстро
> запустить, чтобы при этом не вызывать внезапные ошибки. Например, сделай
> хардкод определенных версий библиотек. Также сделай возможность бесшовно
> перейти на реальный API KudaGo в дальнейшем. После этого еще раз
> проанализируй, все ли сделано и добавлено перед наивным прогоном.

Добавил в `AGENTS.md`/`SPEC.md`: точные версии без диапазонов + `package-lock.json`
+ `npm ci`, статический импорт снапшота вместо `fetch` (ноль сетевых
обращений при демонстрации), проверку peer dependency `react-leaflet`↔React 19
на установке вместо форсирования флагами, и главное — единственный
модуль-загрузчик данных со своим типом `Event`, через который в будущем
меняется источник на живой API без переписывания экрана.

Один пункт не сделал буквально так, как просили: не стал жёстко пинить патч-
версию Node.js. Пользователь предлагал хардкодить версии библиотек «например»
— применил это к npm-зависимостям (там жёсткий пин безопасен, всё внутри
`node_modules` проекта), но не к самому Node.js: чек-лист сдачи требует
«запускается строго по README на чистой машине», а чистая машина проверяющего
почти наверняка использует другой патч Node. Жёсткий пин здесь скорее
сломает установку, чем защитит от неё — записал только нижнюю границу
мажорной версии как рекомендацию.

### Промпт 16
> Начинай

Реализация по спеке, на ветке `main` (наивный прогон остался изолирован в
`naive-run`, не трогался). Ход работы:

1. Скаффолдинг Vite+React+TS сделан не командой `npm create vite@latest .`
   прямо в корне — она интерактивно спрашивает про непустую директорию и
   рискует переписать уже существующие `AGENTS.md`/`SPEC.md`/`sessions`/`data`.
   Вместо этого сгенерировал шаблон в отдельной временной папке и вручную
   перенёс только нужные файлы (`package.json`, `tsconfig*`, `vite.config.ts`,
   `index.html`, `src/main.tsx`, `src/index.css`), пропустив демо-контент
   шаблона (счётчик, `App.tsx`/`App.css`/`assets` с картинками) и его
   `README.md` — свой уже есть, трогать его не время.
2. Обнаружил, что актуальный шаблон Vite (проверено сейчас) использует
   `oxlint` вместо ESLint и заметно более новые версии (React 19.2.8,
   Vite 8.2.0, TypeScript 6.0.2) — не то, что я держал в голове, когда писал
   `AGENTS.md`. Команды (`npm run lint` и т.д.) в `AGENTS.md` уже были
   написаны обобщённо по имени скрипта, а не по инструменту, так что править
   файл не пришлось — но это неплохой пример, почему нельзя фиксировать в
   правилах предположения о версиях, которые сам не проверил.
3. `react-leaflet@5.0.0` — актуальная стабильная версия (не rc) — заявляет
   `react: ^19.0.0` как peer dependency, совпадает без конфликтов. Ставить
   пришлось бы `--legacy-peer-deps` только если бы взял версию 4.x — не
   потребовалось.
4. Данные: перенёс `data/events-snapshot.json` в `src/data/events-snapshot.json`
   (`git mv`), написал единственный модуль-загрузчик `src/data/events.ts` со
   своим типом `AppEvent` — ровно тот шов для будущего живого API, который
   обсуждали заранее.
5. Состояния реализованы через `useEventFilter` — вычисляемое состояние
   (`empty` / `has-data` / `not-found`), а не три независимых булевых флага,
   чтобы исключить недостижимую комбинацию.
6. Для календаря и категории сознательно не стал брать сторонние библиотеки
   (например, `react-day-picker`): в `SPEC.md`/`AGENTS.md` подключение новых
   зависимостей — повод спросить, а самодельный месячный грид на нативных
   `<button>` и нативный `<select>` для категории закрывают требование к
   клавиатурной доступности «бесплатно» — Enter/Space работают из коробки,
   не нужно вручную ловить события и городить `role="listbox"`.

## Проверка (не «готово», а показанный результат)

`npm run build` (tsc -b && vite build) и `npm run lint` (oxlint) — чисто,
без ошибок и предупреждений (вывод команд смотрел целиком, не только код
возврата).

Дальше — визуальная проверка в реальном браузере через `claude-in-chrome`
(процесс дев-сервера поднимал сам через Bash, не через агента — он не должен
знать про фоновые процессы вне своей задачи):

- **Состояние «Пусто»**: открытие страницы без выбранного дня — карта и
  список показывают мероприятия за 2026-08-17 – 2026-08-23, подзаголовок
  соответствует. Совпало с ожиданием из `SPEC.md`.
- **Состояние «Есть данные»**: клик по дню 23 августа — список и карта
  фильтруются до 4 мероприятий этого дня, календарь подсвечивает день,
  появляется кнопка «Сбросить день».
- **Состояние «Ничего не найдено»**: день 17 августа + категория «Детям» —
  сообщение показано, карта пустая, без ошибок.
- **Карточка мероприятия**: клик по метке на карте открывает карточку с
  реальными данными (проверил на «Палеонтологический музей» — координаты
  действительно южная Москва, не баг, а Профсоюзная улица); фокус
  автоматически уходит на кнопку «Закрыть»; `Esc` закрывает.
- **localStorage**: после выбора дня/категории значение реально пишется в
  `localStorage` (проверил через `javascript_tool`, не поверил на слово),
  переживает перезагрузку страницы. Затем **испортил** значение вручную
  (`'{not valid json!!'`) и перезагрузил — экран не упал, тихо откатился в
  состояние «Пусто», в консоли ни одной ошибки. Ровно тот нештатный сценарий
  из `SPEC.md`.
- **Клавиатура**: `Tab` последовательно проходит месяц-навигацию, все дни
  календаря, `select` категории, кнопку сброса, карточки списка мероприятий —
  фокус всегда видим (наша `focus-visible` обводка). `Enter` на дне месяца и
  на карточке списка активирует их так же, как клик (нативные `<button>`
  сработали без дополнительного кода). `Escape` закрывает карточку
  мероприятия что при открытии мышью, что клавиатурой.
- **Ширина 1440**: без горизонтальной прокрутки, список+карта в две колонки.
- **Ширина 360**: `resize_window` браузера физически не может уйти ниже
  ~500px — это ограничение Chrome, а не приложения. Обошёл честно: вставил
  на страницу `<iframe>` шириной ровно 360px через `javascript_tool` (у
  iframe свой независимый viewport, реальные media queries отрабатывают по
  нему), проверил программно `scrollWidth === clientWidth` внутри фрейма —
  горизонтальной прокрутки нет, макет переходит в мобильную раскладку
  (список над картой), календарь не обрезается. Записываю сюда и сам приём
  проверки — он не очевиден и заслуживает быть в журнале, а не только
  результат.
- **Консоль браузера**: `read_console_messages` — ни одной ошибки за всю
  сессию проверки, только служебные DEBUG-сообщения Vite HMR.

## Финальный вердикт (промежуточный, сессия продолжается)

Контекст и спецификация готовы, приняты пользователем устно («Согласен со
всем списком»), затем дважды доуточнены и исправлены по прямым запросам
пользователя. Снапшот данных (18 мероприятий, `data/events-snapshot.json`)
создан из реального источника и сохранён в проекте — пункт «источник данных»
закрыт. Карта на снапшоте визуально не проверена — сделать это первым делом
при вёрстке карты, а не после.

Проверил `AGENTS.md`/`SPEC.md` построчно против `homework.pdf` дважды за
сессию (промпты 13 и 15) — оба раза находились реальные расхождения, не
косметика. Это единственный сигнал, которому я на этом этапе доверяю: сам
факт, что повторная проверка находит новое, означает, что и следующая может
найти ещё — сейчас файлы закрывают всё, что явно требует курс для
Frontend-задания, но stress-test — это наивный/spec-прогон, а не чтение
текста.

Обнаружен пробел вне контента файлов: **папка не является git-репозиторием**
(`git status` → `fatal: not a git repository`). Для сдачи нужен репозиторий с
историей коммитов, а для наивного прогона по правилам курса — «ветка, папка
или коммит с первым результатом», то есть результат наивного прогона должен
физически не смешиваться с версией по спеке. Без git самый естественный
вариант — раздельные папки; с git — отдельная ветка или ранний коммит. Это
нужно решить до того, как запускать наивный прогон, а не после.

Из цикла закрыты: «Контекст → Спецификация», «Наивный прогон» (ветка
`naive-run`, честный, без нашего контекста), «Работа по спеке» — рабочий
экран на `main`, все критерии приёмки из `SPEC.md` прошли проверку с
показанным выводом (см. раздел «Проверка» выше), а не на слово.

**Что осталось не сделано на конец сессии, явно:**
- Дизайн-проход (скриншоты «до/после») по правилам должен идти отдельным
  промптом/сессией от логики — не запускал, текущий вид — рабочая вёрстка
  под требования состояний/ширин, не оформительский проход.
- Независимый аудит (другая модель / чистая сессия) — не проводился.
- `README.md` в корне всё ещё исходный курсовой файл, не наша инструкция по
  запуску — по составу сдачи его нужно переписать.
- `sessions/STATE.md` не обновлён с начала работы — обновлю в конце этой
  сессии, не раньше (иначе он не отражает «как есть сейчас»).
- Автотестов нет вообще — проверка была ручная через браузер за один
  прогон; при повторных правках придётся перепроверять руками же, если не
  появится время на Playwright-сценарий.

Статус сессии — «в работе», ноутбук не закрывали.
