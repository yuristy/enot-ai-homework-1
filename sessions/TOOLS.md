# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-17 · Сессия 1 · Vite 8.2.0 + React 19.2.8 + TypeScript 6.0.2
- **Тип:** фреймворк/сборщик (npm)
- **Установка:** `npm create vite@latest <tmp> -- --template react-ts` в скретчпаде,
  затем вручную перенесены нужные файлы в корень репозитория (не поверх
  существующих AGENTS.md/SPEC.md/sessions/data), версии зафиксированы точно
  (без `^`/`~`) в `package.json`
- **Зачем:** стек проекта по `SPEC.md`
- **Область:** проект
- **Проверка:** `npm run build` (tsc -b && vite build) проходит без ошибок,
  `npm run lint` (oxlint) — 0 предупреждений/ошибок

## 2026-08-17 · Сессия 1 · react-leaflet 5.0.0 + leaflet 1.9.4 + @types/leaflet 1.9.22
- **Тип:** библиотека (npm)
- **Установка:** `npm install --save-exact react-leaflet@5.0.0 leaflet@1.9.4`,
  `npm install --save-exact --save-dev @types/leaflet`
- **Зачем:** отрисовка географической карты с метками мероприятий
- **Область:** проект
- **Проверка:** перед установкой проверил `npm view react-leaflet
  peerDependencies` — 5.0.0 требует `react ^19.0.0`, совпадает с проектом,
  конфликтов и `--legacy-peer-deps` не понадобилось. Визуально проверено в
  браузере (Chrome через claude-in-chrome): маркеры кликабельны, тайлы
  OpenStreetMap грузятся, координаты соответствуют реальным адресам.

## 2026-08-17 · Сессия 1 · claude-in-chrome (skill + MCP-инструменты браузера)
- **Тип:** скилл/MCP
- **Установка:** уже подключён в окружении, вызван через `Skill`
- **Зачем:** визуальная и функциональная проверка экрана вместо слова
  «готово» — три состояния, 360/1440px, клавиатурная доступность,
  localStorage и его порча, консоль без ошибок
- **Область:** сессия (не добавляет зависимостей в проект)
- **Проверка:** сам факт использования и есть проверка — задокументированные
  находки ниже в `session-1.md`

## 2026-08-17 · Сессия 1 · KudaGo public API

- **Тип:** внешний сервис (источник данных)
- **Установка:** не требуется, публичный HTTP API без ключа —
  `https://kudago.com/public-api/v1.4/events/`
- **Зачем:** источник реальных данных о мероприятиях Москвы для снапшота карты
- **Область:** проект (данные, не рантайм-зависимость — снапшот статичный)
- **Проверка:** запрос через `curl` 2026-08-17, ответ 200, JSON с полями
  `place.coords.lat/lon`, `categories`, `dates`. Из 412 подходящих по
  диапазону дат событий выбраны 18 с валидными координатами и категориями
  из «развлекательного» набора (concert/theater/kids/cinema/party/tour/
  entertainment), сохранены в `data/events-snapshot.json`.

## 2026-08-17 · Сессия 2 · скилл frontend-design (Anthropic)
- **Тип:** скилл ассистента
- **Установка:** вручную (не через `/plugin install`, у меня нет доступа к
  слэш-командам маркетплейса) — скачан `SKILL.md` и `LICENSE.txt` с
  `raw.githubusercontent.com/anthropics/skills/main/skills/frontend-design/`
  в `.claude/skills/frontend-design/` (проектный скилл)
- **Зачем:** курс явно показывает разницу в результате дизайн-прохода со
  скиллом и без — используем его для этого шага, а не для основной разработки
- **Область:** проект (`.claude/skills/`, попадёт в git)
- **Проверка:** файл скачан (200, 55 строк), содержание сверено с описанием
  скилла в `materials/reference.md` — совпадает (роль арт-директора,
  калибровка трёх шаблонных AI-дизайнов, двухпроходный процесс с критикой,
  раздел про копирайтинг). Лицензия — Apache 2.0, `LICENSE.txt` скачан
  отдельно.
